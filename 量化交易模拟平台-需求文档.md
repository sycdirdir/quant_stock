# 量化交易模拟平台 — 需求文档 v0.4

> 整理日期：2026-04-26
> 状态：**需求确认完成，待进入系统设计阶段**

---

## 一、项目概述

### 1.1 项目目标

构建一个 **量化交易模拟回测平台**，面向 **期货 + A 股投资者**，用于整理和验证投资思路。Windows 客户端从服务端获取行情数据，下载到本地存储，在本地进行反复回测和策略验证，不依赖网络和服务器算力。

### 1.2 核心使用场景

- **本地回测**：下载数据到本地 → 配置策略 → 运行回测 → 查看结果（反复进行，不消耗服务器资源）
- **时间轴复盘**：选定起始时间，像视频一样逐帧播放，观察策略信号触发那一刻的价格行为
- **策略管理**：策略保存为模板，可复用、可对比、可导出
- **多周期验证**：用日线快速筛选策略方向，用 30分钟/60分钟 精调入场出场点

### 1.3 目标用户

| 用户画像 | 特点 |
|---------|------|
| 期货日内交易者 | 关注分钟线、Tick，频繁策略迭代 |
| A 股波段投资者 | 关注日线/周线，策略验证周期长 |
| 量化爱好者 | 有 Python 基础，需要代码扩展 |

### 1.4 数据规模

| 数据类型 | 规模 | 本地存储 |
|---------|------|---------|
| 日线 | ~5 GB / 5年 / 全市场 | 全部下载到本地 |
| 分钟线（1m/5m/30m/60m） | ~50 GB | 按需下载（单只/板块） |
| Tick | 3 TB（全市场5年） | 对象存储，按需调取，不全部存放本地 |

---

## 二、产品形态

### 2.1 客户端类型

```
┌─────────────────────────────────────────────────────────┐
│                 本地服务器（自建）                          │
│  - 微信 OAuth 认证服务                                    │
│  - K线/日线数据接口（客户端调取下载）                       │
│  - 策略云端同步服务                                       │
│  - Tick 数据（对象存储，按需调取）                         │
└─────────────────────────────────────────────────────────┘
          │
          │  日线/分钟线下载 + Tick 按需调取
          ▼
┌─────────────────────────────────────────────────────────┐
│           Windows 客户端（Electron + React）               │
│  - 数据本地存储（SQLite）                                 │
│  - 本地回测计算                                           │
│  - K线图表展示                                            │
│  - 策略编辑/回测/播放模拟                                  │
│  - 离线可用（联网时同步策略/数据）                          │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| Windows 客户端 | **Electron + React + TypeScript** | 跨平台桌面，UI 体验好 |
| 本地数据库 | **SQLite** | 轻量，文件级存储，本地部署 |
| 图表 | **ECharts 5** | 金融图表成熟方案 |
| 策略引擎 | 自研 Python + Ta-Lib | 可视化条件配置 + Python 代码扩展 |
| 用户认证 | **微信 OAuth 2.0**（扫码登录） | 本地服务器认证 |
| 数据下载 | HTTP API（客户端 → 本地服务器） | 日线/分钟线下载到本地 SQLite |
| Tick 存储 | **对象存储**（S3 兼容） | 按需调取，不全部存放本地 |
| 云端服务 | **Python 3.11 + FastAPI** | 认证 + 数据接口 + 策略同步 |
| 部署 | **Windows 安装包（NSIS）+ Docker（服务端）** | 本地服务器部署 |

---

## 三、系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│              Windows 客户端（Electron + React）            │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │                    UI 层（React）                   │   │
│  │  K线图 | 策略编辑器 | 回测面板 | 播放控制器          │   │
│  └────────────────────────┬────────────────────────┘   │
│                           │                              │
│  ┌────────────────────────▼────────────────────────┐   │
│  │              状态管理层（Zustand）                  │   │
│  └────────────────────────┬────────────────────────┘   │
│                           │                              │
│  ┌────────────────────────▼────────────────────────┐   │
│  │              本地业务层（Node.js 主进程）            │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────┐  │   │
│  │  │ 数据管理服务   │  │ 策略执行引擎  │  │ 风控模块│  │   │
│  │  │ (下载/存储)  │  │ (回测/播放)   │  │ 止损/止盈│ │   │
│  │  └──────────────┘  └──────────────┘  └────────┘  │   │
│  └────────────────────────┬────────────────────────┘   │
│                           │                              │
│  ┌────────────────────────▼────────────────────────┐   │
│  │              本地数据库（SQLite）                  │   │
│  │  stocks | kline_d | kline_w | kline_m           │   │
│  │  kline_30m | kline_60m | strategy_templates     │   │
│  │  backtest_results                               │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
          │ HTTP API（下载数据 / 微信认证 / 策略同步）
          │ Tick 按需调取（对象存储）
          ▼
┌─────────────────────────────────────────────────────────┐
│              本地服务器（自建，Docker 部署）               │
│                                                          │
│  ┌────────────────┐  ┌─────────────────────────────┐   │
│  │ 微信 OAuth 服务  │  │  数据服务（K线/分钟线下载）   │   │
│  │  - 扫码登录      │  │  - 日线/分钟线 HTTP 接口     │   │
│  │  - JWT 签发     │  │  - 增量更新                 │   │
│  └────────────────┘  └─────────────────────────────┘   │
│                                                          │
│  ┌────────────────┐  ┌─────────────────────────────┐   │
│  │ 策略云端同步服务  │  │ 对象存储（Tick 数据）         │   │
│  │  - 备份/恢复     │  │  - 按需调取（按股票/日期）    │   │
│  └────────────────┘  │  - 不全部存本地                │   │
│                      └─────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 3.2 数据流转

```
[PostgreSQL / 对象存储]
         │
         │ 每日收盘后
         ▼
[本地服务器] ──HTTP API──> [Windows 客户端] ──存储──> [本地 SQLite]
                              │
                              │ 联网时同步
                              ▼
                        [策略云端备份]
```

---

## 四、数据模型

### 4.1 本地数据库（SQLite）

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `stocks` | 股票/期货基础信息 | `stock_code`, `name`, `market`, `type`（股票/期货）|
| `kline_1m` ~ `kline_60m` | 分钟K线 | `stock_code`, `datetime`, `open`, `high`, `low`, `close`, `volume`, `amount` |
| `kline_d` | 日K线 | 同上 |
| `kline_w` | 周K线 | 同上 |
| `kline_m` | 月K线 | 同上 |
| `strategy_templates` | 策略模板 | `id`, `user_id`, `name`, `config_json`, `code`, `created_at` |
| `backtest_results` | 回测结果 | `id`, `strategy_id`, `stock_code`, `result_json` |
| `sync_log` | 同步记录 | `last_sync_time`, `synced_stocks`, `version` |

### 4.2 云端数据库（PostgreSQL）

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `users` | 用户（微信关联） | `id`, `openid`, `nickname`, `avatar_url` |
| `cloud_strategies` | 云端策略备份 | `user_id`, `strategy_json`, `updated_at` |
| `data_update_records` | 数据更新记录 | `date`, `updated_stocks`, `version` |
| `stocks_meta` | 股票/期货元数据 | `stock_code`, `name`, `market`, `type` |

### 4.3 API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `POST /api/auth/wechat/login` | POST | 微信登录（code 换 JWT） |
| `GET /api/auth/me` | GET | 获取当前用户 |
| `GET /api/stocks` | GET | 获取股票/期货列表 |
| `GET /api/data/updates` | GET | 获取增量更新列表（日期范围） |
| `GET /api/data/download/{stock_code}` | GET | 下载指定股票数据（指定周期/日期范围） |
| `GET /api/data/tick/{stock_code}` | GET | 从对象存储获取 Tick 数据（按需） |
| `POST /api/cloud/strategies/sync` | POST | 同步策略到云端 |
| `GET /api/cloud/strategies` | GET | 获取云端策略列表 |

---

## 五、功能模块

### 5.1 用户认证（微信扫码登录）

- 客户端启动 → 显示微信扫码（调用本地服务器微信 OAuth）
- 扫码授权后返回 JWT Token，存储在客户端
- 登录后可同步云端策略、下载最新数据
- Token 有效期 7 天，自动续期
- 支持离线使用（无需持续联网，回测全部本地进行）

### 5.2 数据管理

- **数据下载**：客户端选择股票/期货品种 → 选择时间范围 → HTTP 下载 → 存入本地 SQLite
- **增量更新**：联网时检测服务器增量，仅下载新增数据
- **Tick 数据**：从对象存储按需调取（指定股票 + 日期范围），不全部存本地
- **数据清理**：按需删除本地数据，释放磁盘空间
- **离线使用**：数据下载完成后，完全离线可用

### 5.3 K 线图表展示（ECharts）

- **蜡烛图（K线）**：红涨绿跌，支持缩放、拖拽
- **均线叠加**：**MA5、MA10、MA20**（默认显示，可配置隐藏）
- **成交量柱状图**：底部子图，红涨绿跌
- **MACD**：底部子图（DIF、DEA、MACD柱）
- **KDJ**：底部子图（K、D、J线）
- **时间轴播放光标**：在图表上标记当前播放位置
- **买卖信号标记**：买入（绿色▲）、卖出（红色▼）
- **资金曲线叠加**：可选，叠加在K线图

### 5.4 策略配置（混合模式）

#### 5.4.1 可视化条件配置

**信号触发条件：**
- 均线交叉：MA5 上穿/下穿 MA10、MA10 上穿/下穿 MA20
- KDJ 超买超卖：K 值 > 80 死叉 / K 值 < 20 金叉
- MACD 交叉：DIF 上穿/下穿 DEA
- 价格突破：收盘价 > N 日最高价 / 收盘价 < N 日最低价
- 成交量异常：成交量 > MA5 的 2 倍

**条件组合**：AND / OR 逻辑组合

#### 5.4.2 Python 代码策略扩展

高级用户编写策略函数，平台提供 Monaco Editor 代码编辑器。

```python
def strategy(ctx: StrategyContext) -> Signal:
    """
    ctx 提供: close_prices, volumes, datetime, position, cash
    返回: Signal.BUY / Signal.SELL / Signal.HOLD
    """
```

### 5.5 策略模板管理

- 保存策略为模板（命名、描述）
- 模板列表（查看、编辑、删除、克隆）
- **云端同步**（联网时自动同步到云端备份）
- 本地存储 + 云端备份双保险

### 5.6 快速回测

- 选择股票/期货、时间范围（最长 1 年）、数据周期
- 配置策略参数（均线周期：MA5、MA10、MA20）
- 配置风控参数（止损%、止盈%、仓位）
- 一键运行全量回测（本地计算，不占服务器资源）
- **初始资金：10 万元**

**回测指标：**

| 指标 | 说明 |
|------|------|
| 总收益率 | (最终净值 - 10万) / 10万 |
| 年化收益率 | 总收益 / 回测年数 |
| 夏普比率 | (年化收益 - 无风险利率) / 年化波动率 |
| 最大回撤 | 从峰值到谷底的最大跌幅 |
| 胜率 | 盈利交易次数 / 总交易次数 |
| 盈亏比 | 平均盈利 / 平均亏损 |
| 交易次数 | 总买卖次数 |
| 平均持仓时长 | 每笔交易平均持仓时间 |

**结果可视化：**
- 收益曲线图（资金曲线）
- 买卖点位图（标注在K线图上）
- 月度收益分布

### 5.7 时间轴模拟播放

- 选定起始时间点，点击"播放"
- 日线/30分钟/60分钟 按帧推进（速度：1x / 2x / 5x / 10x）
- Tick 播放（速度更快，适合日内交易复盘）
- 实时计算均线、MACD、KDJ
- **策略条件触发时，实时弹窗提示**（买入/卖出信号）
- 支持暂停、步进（前进一步/后退一步）

### 5.8 风控模块

每个策略必须配置风控规则：

| 功能 | 默认值 | 说明 |
|------|-------|------|
| 止损 | 5% | 亏损达到 X% 自动卖出 |
| 止盈 | 10% | 盈利达到 X% 自动卖出 |
| 仓位管理 | 每次 2 万元 | 固定金额 M 元 |
| 单日最大交易次数 | 3 次 | 防止过度交易 |
| 最大持仓比例 | 30% | 不超过总资金的 X% |

### 5.9 回测报告导出

- 导出 PDF 回测报告（含图表、指标、交易明细）
- 导出交易明细 Excel

---

## 六、非功能需求

| 需求 | 说明 |
|------|------|
| 性能（日线回测） | 1年日线数据回测 < 5秒 |
| 性能（分钟线回测） | 1年30分钟线 < 30秒 |
| 内存占用 | 客户端运行时 < 2 GB（含1年K线数据） |
| 磁盘占用 | 全市场日线5年约 5 GB；分钟线按需 |
| 响应式 | 支持 1920x1080 及以上 |
| 客户端系统 | Windows 10/11 |
| 部署 | Windows 安装包（NSIS）+ Docker（服务端） |
| 离线支持 | 完全支持，数据本地，不依赖服务器 |

---

## 七、优先级排序

### P0 — MVP（第一阶段）

> **目标**：Windows 客户端可下载日线数据，完成基础回测验证

1. Windows 客户端框架（Electron + React + SQLite）
2. 微信扫码登录（本地服务器 OAuth）
3. 股票/期货数据下载管理（服务端 → 本地 SQLite）
4. K线图展示（蜡烛图 + MA5/MA10/MA20）
5. 均线交叉策略可视化配置
6. 快速回测 + 核心指标（收益率、最大回撤、夏普、胜率）
7. 买卖信号标注在K线图
8. 止损/止盈风控
9. 策略模板保存/加载（本地）
10. 策略云端同步（本地服务器）

### P1 — 重要功能（第二阶段）

1. MACD / KDJ 指标
2. 成交量子图
3. 时间轴播放模拟（日线/30分钟/60分钟）
4. 资金曲线叠加
5. 盈亏比、平均持仓时长补充指标
6. 云端策略备份与恢复

### P2 — 增强功能（第三阶段）

1. Python 代码策略扩展（Monaco Editor）
2. Tick 数据调取 + Tick 级别播放（对象存储按需调取）
3. 导出 PDF / Excel 报告
4. 多周期切换（1分钟/5分钟/30分钟/60分钟/日线/周线/月线）
5. 基准（沪深300/商品指数）对比
6. 月度收益分布图
7. 策略参数优化（网格搜索）

---

## 八、需求确认汇总

| # | 事项 | 确认内容 |
|---|------|---------|
| 1 | 数据格式/量级 | H5导入PG；Tick 3TB，在对象存储，按需调取 |
| 2 | MVP 数据范围 | **日线优先**，Tick P2 阶段 |
| 3 | 目标用户 | 期货 + A 股投资人，整理投资思路 |
| 4 | 产品形态 | **Windows 客户端优先**（Electron + React） |
| 5 | 客户端架构 | 数据从服务端下载到本地 SQLite，离线可用 |
| 6 | 后端技术栈 | Python FastAPI ✓ |
| 7 | 前端技术栈 | React + TypeScript + ECharts ✓ |
| 8 | 均线默认周期 | MA5、MA10、MA20 |
| 9 | 策略模板 | 需要保存，云端同步 ✓ |
| 10 | 用户认证 | 微信 OAuth（公司资质）✓ |
| 11 | 部署方式 | Windows 安装包 + 本地服务器 Docker ✓ |
| 12 | 回测初始资金 | 10 万元 ✓ |
| 13 | 数据初始来源 | 通过服务端 HTTP API 下载到本地 ✓ |
| 14 | Tick 存储 | 对象存储，按需调取，不全部存本地 ✓ |

---

## 九、后续步骤

1. ✅ 需求文档确认（本文档）
2. **下一步：系统设计阶段**

   - Windows 客户端详细架构设计（Electron 进程模型、SQLite 方案）
   - 服务端详细架构设计（API 接口定义、数据库表设计、Docker 配置）
   - 回测引擎详细设计（信号计算、风控执行、指标计算）
   - 微信 OAuth 接入流程设计
   - Tick 对象存储调取方案

3. **开发阶段**（P0 → P1 → P2）

---

*文档版本记录：*
- v0.1 (2026-04-26) — 初始草稿
- v0.2 (2026-04-26) — 补充微信登录、Tick 3TB 数据架构
- v0.3 (2026-04-26) — 架构重大调整：Windows 客户端优先 + 本地数据 + 离线回测
- v0.4 (2026-04-26) — 需求全部确认，客户端 Electron+React，数据服务端API下载，Tick对象存储按需调取
- v0.5 (2026-04-26) — **对接实际环境**，确认现有数据（日线✅ 分钟线待H5导入），MVP日线先行

---

## 十、实际环境对接

### 10.1 数据库环境

**PostgreSQL（10.168.1.112）— tushare 库：**

| 表名 | 数据量 | 状态 |
|------|--------|------|
| `stock_basic` | 5,493 | ✅ 可用 |
| `daily` | 1,234,263（2020-01-02 ~ 2026-04-24） | ✅ 可用 |
| `stock_weekly` | 211,354 | ✅ 可用 |
| `stock_monthly` | 56,578 | ✅ 可用 |
| `stock_1min/5min/30min/60min` | 0 | ❌ 待H5导入 |
| `index_1min` | 8,000 | ✅ 可用 |
| `index_5min/15min/30min/60min` | 0 | ❌ 待H5导入 |
| `index_daily` | 17,878 | ✅ 可用 |
| `targetlist` | 5,429 活跃标的 | ✅ 可用 |

**注意**：分钟级数据待H5文件导入后再使用，MVP阶段只用日线数据。

### 10.2 应用服务器（10.168.0.189）

- Nginx 已在 80/443 端口运行（可用于反向代理）
- Python 3 环境可用（psycopg2 已装）
- Docker 可用（需权限）
- TradebySyc 项目（选股脚本），非本平台代码

### 10.3 MVP 数据映射

| 需求文档周期 | 数据库表 | MVP状态 |
|------------|---------|--------|
| 日线 | `daily` | ✅ |
| 周线 | `stock_weekly` | ✅ |
| 月线 | `stock_monthly` | ✅ |
| 1分钟 | `stock_1min` | ❌ 待H5 |
| 5分钟 | `stock_5min` | ❌ 待H5 |
| 30分钟 | `stock_30min` | ❌ 待H5 |
| 60分钟 | `stock_60min` | ❌ 待H5 |
| Tick | — | ❌ 对象存储
